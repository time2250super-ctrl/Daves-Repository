# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must stay LF so the shebang works on Linux.
*.sh text eol=lf
# PowerShell scripts are Windows-only helpers.
*.ps1 text eol=crlf
